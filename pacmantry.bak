#include<iostream.h>
#include<conio.h>

void main()
{
   int a=0,b,c,d;
	for(a=0;a<25;a++)
   {
   	gotoxy(0,a);
      cout<<"|";
      gotoxy(80,a);
      cout<<"|";
   }
   for(b=0;b<80;b++)
   {
   	gotoxy(b,25);
      cout<<"-";
      gotoxy(b,1);
      cout<<"-";
   }
   for(c=1;c<79;c++)
   {
   	gotoxy(c,13);
      cout<<"-";
   }
   for(d=2;d<25;d++)
   {
      gotoxy(40,d);
      cout<<"|";
   }
   getch();
}
